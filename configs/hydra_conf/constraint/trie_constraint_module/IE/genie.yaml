# @package constraint.trie_constraint_module
defaults:
  - /constraint/trie_constraint_module/_from_constrained_world_default@_here_

constrained_world_id: genie # specifies the folder name from which the constrained world is loaded
identifier: genie # specifies the cache subfolder where the trie will be stored

linearization_class_id: ${datamodule.linearization_class_id}
